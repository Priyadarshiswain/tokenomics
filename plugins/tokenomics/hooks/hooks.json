{
  "description": "Warn once when session context passes the point where a /compact starts paying for itself. Cost tier only — hooks are not given the context window size, so the capacity tier lives in the status line.",
  "hooks": {
    "Stop": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "${CLAUDE_PLUGIN_ROOT}/hooks/compact-nudge.sh",
            "timeout": 5
          }
        ]
      }
    ]
  }
}
