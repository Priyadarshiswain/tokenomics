{
  "description": "Nudge once per 150k of session context — an escalating ladder that re-arms after a compact. Cost tier only — hooks are not given the context window size, so the capacity tier lives in the status line.",
  "hooks": {
    "Stop": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "python3 \"${CLAUDE_PLUGIN_ROOT}/hooks/compact-nudge.py\"",
            "timeout": 5
          }
        ]
      }
    ]
  }
}
