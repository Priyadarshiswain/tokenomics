{
  "$schema": "https://anthropic.com/claude-code/marketplace.schema.json",
  "name": "tokenomics",
  "description": "Session tokenomics for Claude Code",
  "owner": { "name": "Priyadarshi Swain" },
  "plugins": [
    {
      "name": "tokenomics",
      "description": "Measure where a Claude Code session's tokens and context actually go, per call and per session, as an HTML report or a live status line. Token counts only, no pricing.",
      "source": { "source": "directory", "path": "." }
    }
  ]
}
